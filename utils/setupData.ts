// This file has been removed as part of the simplification to filesystem-only architecture.
// Sample data setup is no longer needed as the application uses file storage only.

// Placeholder function to prevent import errors
export const setupSampleData = async () => {
  console.log('Sample data setup is no longer available in filesystem-only mode');
};